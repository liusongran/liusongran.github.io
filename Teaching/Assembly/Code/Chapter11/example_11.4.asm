DSEG	SEGMENT
LIST	DB		63,52,11,96,2
LLNT	DB		5
ORD		DB		11
DSEG	ENDS

CSEG	SEGMENT
		ASSUME	CS:CSEG,DS:DSEG,ES:DSEG
START:	MOV		AX,DSEG
		MOV		DS,AX
		MOV		ES,AX
		XOR		CH,CH
		MOV		AL,ORD
		MOV		CL,LLNT
		LEA		DI,LIST
		MOV		SI,DI
		ADD		SI,CX
		DEC		SI
DEL1:	CMP		AL,[DI]
		JZ		DEL2
		INC		DI
		DEC		CL
		JNZ		DEL1
		STC
		JMP		STOP
DEL2:	CMP		SI,DI
		JZ		DEL3
		MOV		SI,DI
		INC		SI
		CLD
		REP		MOVSB
DEL3:	DEC		LLNT
STOP:	MOV		AH,4CH
		INT		21H
CSEG	ENDS
		END		START
